# MongoDB Init

This folder contains a MongoDB initialization script for Fresher Connect.

Collections created:

- `users`
- `companies`
- `jobs`
- `applications`
- `counters`

Run it with:

```bash
mongosh < mongodb/init.js
```

Or:

```bash
mongosh --file mongodb/init.js
```
